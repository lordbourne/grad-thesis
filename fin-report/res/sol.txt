解决方案
=====

# 页面间的数据传递
SPA？LocalStorage? 缓存？


# 缓存机制



## 图标颜色的改变方法整理
- CSS3 滤镜
- 图标字体
    - web font 简介

本项目中的运用

## 项目架构设计
几个文件夹，图标与图片是否分开放
不用分开

## 图片资源的安排
图片是否放到其他服务器存储？
本地服务器存储的优点、缺点
专门的服务器存储的优点、缺点

## 缓存的安排
各种浏览器的本地存储的对比，浏览器的支持情况等

## 数据填充方法

正则模板填充速度更快，因为是纯字符串操作，不需要像 dom 操作那样不断获取 dom 元素

- dom 操作
- 正则表达式模板

正则模板适合静态数据，就是模板中要填充的数据的 key 已经提前填好，个数已经固定，只需三步操作，
第一步：整个加载模板
第二步：填充数据
第三步：生成 dom 对象插入到页面中

如果是动态数据，即 html 中某些标签的个数需要根据从服务器接受到的 json 数据来判断，此时，如果采用正则填充技术，需要在

各自的优缺点

## 数据加载过程中的特效

## 轮播、幻灯片特效

## 如何判断未定义值
!localStorage.city == true
typeof localStorage.city == "undefined"

# 首页的各种解决方案
模板填充，用 data- 属性
文字省略

链接字符串的方法：
1. 'a' + 'b' + 'c'
2. [].join() 注意，.join() 很容易忘记

## 过长的文字如何省略  CSS3

## viewport 问题
如果不设置 viewport 会出现很严重的问题，主要是尺寸问题，自己举例分析

# 艾灸文化页的解决方案
文章的书写平台，需要单独搭建博客平台
该博客能够提取文章的各项信息，作者、


# 字体方案
本项目的字体系统中不存在，发布的时候可能需要利用 Web font

## 考不考虑离线应用
模板存储

## $.ajax() 获取 Json 的一个 bug
$.ajax() 方法的 data 参数一定要有，可以为空, 如 data: {}
请求的 Json 数据的文件名中不能有中文

## jq 如何隐藏元素
.hide() or addClass()
区别

## js 变量可以重定义吗
